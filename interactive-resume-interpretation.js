function moveLayers() {
  // setLayersMovement
    // get/set pageVerticalPosition
    // function detectPageVerticalPosition
      // set current pageVerticalPosition to previousPageVerticalPosition
      // get distance already scrolled vertically in pixels
      // if pageVerticalPosition < 0, reset position to 0
      // check if pageVerticalPosition is > than (height of the page div - the height of the container div)
    // set horizontal and vertical speed
}
